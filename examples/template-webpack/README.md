# template-webpack-direct

This example shows how to keep using `rspeedy build` without relying on `pluginReactLynx()`, and directly assemble a Lynx bundle from `background.js`, `main-thread.js`, and CSS data through `@lynx-js/template-webpack-plugin`.
