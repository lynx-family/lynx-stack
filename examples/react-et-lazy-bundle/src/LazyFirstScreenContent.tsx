import './LazyComponent.css';

export default function LazyFirstScreenContent() {
  return (
    <view className='LazyPanel'>
      <text className='LazyComponent'>first-screen-ready</text>
    </view>
  );
}
