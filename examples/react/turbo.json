{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src", "lynx.config.js"],
      "outputs": ["dist/**"],
      "env": ["CODECOV_TOKEN", "GITHUB_SHA", "CI"]
    }
  }
}
