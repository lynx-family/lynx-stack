---
title: '@lynx-js/genui/lynx-xml'
---

# `@lynx-js/genui/lynx-xml`

`@lynx-js/genui/lynx-xml` 提供系统提示词，让模型用 Vanilla Lynx 和 Element PAPI 写出完整、无需构建的 `.lynxml` 文件；也提供把格式良好的 XML 片段编译成确定性 Element PAPI JavaScript 的工具函数。

```ts
import { buildLynxXmlSystemPrompt } from '@lynx-js/genui/lynx-xml';
```

## API

{/* @api ApiExports package="genui-lynx-xml" */}

## 函数 \{#functions\}

### `buildLynxXmlSystemPrompt()` \{#buildlynxxmlsystemprompt\}

```ts
function buildLynxXmlSystemPrompt(options: BuildLynxXmlSystemPromptOptions): string
```

构建用于生成完整、免构建的 `.lynxml` 文件的系统提示词。

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `options` | [`BuildLynxXmlSystemPromptOptions`](#buildlynxxmlsystempromptoptions) |  |

**返回值：** `string`

### `compileLynxXmlFragment()` \{#compilelynxxmlfragment\}

```ts
function compileLynxXmlFragment(source: string): { text: string; xmlFragment: string }
```

编译模型输出的中间文档，不执行模型代码。

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `source` | `string` |  |

**返回值：** `{ text: string; xmlFragment: string }`

### `generateMainThreadScript()` \{#generatemainthreadscript\}

```ts
function generateMainThreadScript(xmlFragment: string): string
```

把 XML 元素片段转换为供 renderPage() 使用的 Element PAPI 调用。

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `xmlFragment` | `string` |  |

**返回值：** `string`

### `generateMainThreadScriptResult()` \{#generatemainthreadscriptresult\}

```ts
function generateMainThreadScriptResult(xmlFragment: string): GeneratedMainThreadScript
```

生成 Element PAPI 调用，以及稳定的 id 到节点的绑定。

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `xmlFragment` | `string` |  |

**返回值：** [`GeneratedMainThreadScript`](#generatedmainthreadscript)

## 常量 \{#constants\}

### `LYNX_XML_ENGINE_VERSION` \{#lynx-xml-engine-version\}

```ts
const LYNX_XML_ENGINE_VERSION: "4.2"
```

生成的 XML 产物默认使用的 Lynx 引擎版本。

### `LYNX_XML_HTML_FRAGMENT_INSTRUCTIONS` \{#lynx-xml-html-fragment-instructions\}

```ts
const LYNX_XML_HTML_FRAGMENT_INSTRUCTIONS: "XML fragment mode is enabled for this request (this output contract overrides the imported document guidance below):\n- Generate the entire document in one response: one <template> containing the initial XML element fragment directly inside <lynx>, alongside CSS and main/background scripts in their normal source blocks. Prefer placing the template first, but block order is not significant. Never omit the template, even when conversation history contains already-compiled .lynxml artifacts. The server removes <template> and compiles it to Element PAPI before delivery; it is an intermediate format, not a runtime Lynx element.\n- Give a unique id ONLY to nodes referenced later by event binding, state updates, or cleanup. Omit id on purely static nodes; do not assign ids to every node. Use well-formed XML, literal attributes and XML entities in the template. Keep style, script, lynx, and page elements outside the fragment. Prefer literal text directly inside <text>. An explicit <raw-text> leaf may use text content or a text attribute, never both; put styling, event handlers, and update ids on its parent <text>. Do not use interpolation, loops, conditional directives, or inline event-handler attributes; implement dynamic behavior in JavaScript.\n- The server supplies createFragment(page, pageId). Call it exactly once in renderPage(), after page and pageId exist: nodes = createFragment(page, pageId). Declare let nodes at main-thread script scope so later event, update, and cleanup handlers can use nodes[\"cityText\"] for id=\"cityText\". Access nodes only after rendering. Do not declare or shadow createFragment, invent nodeN variables, or assume XML ids declare variables.\n- createFragment creates and appends the initial roots to page and returns their id-to-node map. Do not recreate or append the initial roots yourself. Bind events and apply initial state after the call. Use Element PAPI for subsequent dynamic updates and new nodes.\n- Write all CSS, state, event handlers, lifecycle registration, background work, and cleanup in the same response. Do not request conversion, wait for bindings, or output placeholders. The server performs conversion after generation without another model request."
```

用于确定性片段编译的中间源码约定。

### `LYNX_XML_HTML_FRAGMENT_SYSTEM_PROMPT` \{#lynx-xml-html-fragment-system-prompt\}

```ts
const LYNX_XML_HTML_FRAGMENT_SYSTEM_PROMPT: string
```

一次性生成片段所用的 Lynx XML 提示词。

### `LYNX_XML_SYSTEM_PROMPT` \{#lynx-xml-system-prompt\}

```ts
const LYNX_XML_SYSTEM_PROMPT: string
```

默认的 Lynx XML 生成系统提示词。

### `MAX_XML_FRAGMENT_LENGTH` \{#max-xml-fragment-length\}

```ts
const MAX_XML_FRAGMENT_LENGTH: 100000
```

*暂无说明。*

## 类型 \{#types\}

### `BuildLynxXmlSystemPromptOptions` \{#buildlynxxmlsystempromptoptions\}

用于自定义 Lynx XML 生成系统提示词的选项。

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| <a id="buildlynxxmlsystempromptoptions-appendix"></a>`appendix?` | `string` | 在内置约定之后追加调用方特定的指令。 |
| <a id="buildlynxxmlsystempromptoptions-enablehtmlfragment"></a>`enableHtmlFragment?` | `boolean` | 生成一份中间文档，用于确定性的片段编译。 |
| <a id="buildlynxxmlsystempromptoptions-engineversion"></a>`engineVersion?` | `string` | 覆盖生成产物的 Lynx 引擎版本。 |

### `GeneratedMainThreadScript` \{#generatedmainthreadscript\}

*暂无说明。*

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| <a id="generatedmainthreadscript-bindings"></a>`bindings` | `Record<string, string>` | 运行时的 nodeMap 只保留显式声明的 XML id。 |
| <a id="generatedmainthreadscript-javascript"></a>`javascript` | `string` |  |

{/* @api-end */}
