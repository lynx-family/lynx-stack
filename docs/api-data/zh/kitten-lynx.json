{
  "CDPChannel.summary": {
    "hash": "a4929d79",
    "text": "一个无状态的 CDP（Chrome DevTools Protocol）通道，用于向指定的 Lynx 会话发送命令。\n\n每次 `send()` 调用都是通过 `Connector` 发起的一次短暂的请求/响应。通道按会话 ID 通过 `WeakRef` 缓存，以便复用。"
  },
  "CDPChannel.constructor.summary": {
    "hash": "eb93e6ae",
    "text": "构造一个严格绑定到单个 Lynx 会话的 CDP 通道。\n\n**Agent 须知：**\n优先使用 `CDPChannel.from()`，而不是手动 new 一个通道，因为 `from()` 会处理 WeakRef 缓存，尽量减少内存占用。"
  },
  "CDPChannel.constructor.params._connector": {
    "hash": "acb96262",
    "text": "底层 ADB 传输控制器。"
  },
  "CDPChannel.constructor.params._clientId": {
    "hash": "baa3e150",
    "text": "绑定的 devtool 目标标识字符串。"
  },
  "CDPChannel.constructor.params._sessionId": {
    "hash": "8021ed26",
    "text": "范围限定的 Lynx 视图会话 ID。"
  },
  "CDPChannel.send.summary": {
    "hash": "da34c7d9",
    "text": "发送一条强类型的 Chrome DevTools Protocol（CDP）方法命令，并等待其返回结果。\n\n**Agent 用法：**\n这是 `kitten-lynx` 中所有交互的核心。它不维护 socket 连接，每次 `send(...)` 调用都会构造一个独立的 ADB 请求，并等待对应的独立响应。\n\n**类型：**\n注意，泛型严格绑定到本文件顶部定义的 `Protocol` 接口。如果需要调用 TypeScript 拒绝的 CDP 命令，必须先更新 `Protocol` 接口！"
  },
  "CDPChannel.send.params.method": {
    "hash": "77437be3",
    "text": "要发送给 Lynx devtool 服务器的命令。"
  },
  "CDPChannel.send.params.params": {
    "hash": "a827811f",
    "text": "CDP 方法所需的强类型参数对象。"
  },
  "CDPChannel.from.summary": {
    "hash": "6adf6013",
    "text": "为指定的 Lynx 会话获取或创建一个无状态的 `CDPChannel`。\n\n**设计模式（Agent 须知）：**\n本测试库采用**无状态**的 CDP 架构，以避免在不稳定的 ADB 链路上出现挂起的 WebSocket 连接。通道按 `sessionId` 通过 `WeakRef` 缓存，以减少内存分配。"
  },
  "CDPChannel.from.params.sessionId": {
    "hash": "c1273a79",
    "text": "Lynx 运行时分配的 Lynx devtool 数字会话 ID。"
  },
  "CDPChannel.from.params.clientId": {
    "hash": "bd4ef65c",
    "text": "客户端应用的唯一设备端口标识（例如 `\"emulator-5554:40121\"`）。"
  },
  "CDPChannel.from.params.connector": {
    "hash": "c94f5090",
    "text": "提供 ADB 传输的活动 `Connector` 实例。"
  },
  "ElementNode.summary": {
    "hash": "c95e1fbe",
    "text": "表示 Lynx 页面中的一个 DOM 元素。\n\n封装一个 CDP `nodeId`，并提供检查属性、计算样式以及模拟点击等用户交互的方法。"
  },
  "ElementNode.constructor.summary": {
    "hash": "9d86f859",
    "text": "初始化一个新的 `ElementNode` 实例，用于表示一个 Lynx 组件或标签。\n\n**Agent 须知：**\n与 `KittenLynxView` 类似，很少需要直接调用它。请通过 `KittenLynxView.locator()` 或 `ElementNode.fromId()` 创建实例。"
  },
  "ElementNode.constructor.params.nodeId": {
    "hash": "183e7416",
    "text": "在 Lynx 渲染器中标识该元素的唯一 CDP 数字 ID。"
  },
  "ElementNode.constructor.params._lynxView": {
    "hash": "49dbc670",
    "text": "所属的 `KittenLynxView` 实例，用于发送后续的 CDP 查询。"
  },
  "ElementNode.nodeId.summary": {
    "hash": "183e7416",
    "text": "在 Lynx 渲染器中标识该元素的唯一 CDP 数字 ID。"
  },
  "ElementNode.computedStyleMap.summary": {
    "hash": "da56749d",
    "text": "获取该元素当前生效的全部计算后 CSS 属性。\n\n**Agent 用法：**\n基于 `CSS.getComputedStyleForNode`。返回布局计算后完全解析的样式值（例如将 `100%` 解析为绝对的 `px` 值）。强烈建议用它做视觉断言（例如验证某个盒子确实是 `display: none`，或具有特定的 `background-color`）。"
  },
  "ElementNode.getAttribute.summary": {
    "hash": "0f15f5d8",
    "text": "获取该元素上指定属性的字符串值。\n\n**Agent 注意事项 / 陷阱：**\n在 Lynx 中，Web 标准的 `id=\"foo\"` 属性在内部实际存储为 `idSelector=\"foo\"`。本库对此做了透明兼容：查询 `getAttribute('id')` 时会自动改为查询 `idSelector`，无需自行处理 `idSelector`。"
  },
  "ElementNode.getAttribute.params.name": {
    "hash": "697544ac",
    "text": "要获取的属性名。"
  },
  "ElementNode.tap.summary": {
    "hash": "7e3dfc9d",
    "text": "在该元素中心直接模拟一次原生用户点击（按下后抬起）。\n\n**内部机制（Agent 须知）：**\n这不是模拟的 DOM 事件（如 Web 中的 `element.click()`），而是在原生层精确派发的手势：\n1. 通过 `DOM.getBoxModel` 获取精确的边界坐标。\n2. 计算内容盒的绝对中心点 `(x, y)`。\n3. 通过 ADB 桥发送 `Input.emulateTouchFromMouseEvent`（`'mousePressed'`）。\n4. 等待 50ms，再发送对应的 `'mouseReleased'`。\n\n**重要：** 调用前请确保元素在屏幕上可见，否则坐标计算可能失败，或点击不到任何元素。"
  },
  "ElementNode.fromId.summary": {
    "hash": "9fafe4af",
    "text": "获取或创建与指定 Lynx DOM 节点 ID 对应的 `ElementNode` 实例。\n\n**Agent 缓存策略：**\n节点按 `KittenLynxView` 实例通过 `WeakRef` 积极缓存。这意味着如果 agent 在垃圾回收之前两次查询同一个 DOM 节点，会得到同一个 `ElementNode` 引用，从而在大量遍历 DOM 时降低内存开销。"
  },
  "ElementNode.fromId.params.id": {
    "hash": "a6d5ea6e",
    "text": "要封装的 CDP 数字节点 ID。"
  },
  "ElementNode.fromId.params.lynxView": {
    "hash": "247f9369",
    "text": "该节点所属的 `KittenLynxView` 实例。"
  },
  "KittenLynxView.summary": {
    "hash": "bd2cba8b",
    "text": "表示一个 Lynx 页面实例，类似于 Puppeteer 的 `Page`。\n\n提供导航到 Lynx bundle URL、查询 DOM 以及读取页面内容的方法。通过 [`Lynx.newPage`](api:Lynx.newPage) 创建。"
  },
  "KittenLynxView.constructor.summary": {
    "hash": "79c387bc",
    "text": "初始化一个新的 `KittenLynxView` 实例。\n\n**Agent 须知：**\n通常应避免直接调用此构造函数，而应使用 `Lynx.newPage()`，以正确初始化一个绑定到当前 ADB 连接和客户端的 `KittenLynxView` 实例。"
  },
  "KittenLynxView.constructor.params._connector": {
    "hash": "993c5f9a",
    "text": "底层 `Connector` 实例，用于通过 ADB/USB 发送 CDP 消息。"
  },
  "KittenLynxView.constructor.params._clientId": {
    "hash": "b7f2a3e9",
    "text": "唯一的客户端标识（通常为 `<deviceId>:<port>`）。"
  },
  "KittenLynxView.constructor.params._client": {
    "hash": "45aa714c",
    "text": "可选的客户端元数据对象，包含应用内部状态。"
  },
  "KittenLynxView.content.summary": {
    "hash": "bd70ec46",
    "text": "将当前页面的整个 DOM 树序列化为类 HTML 的字符串。\n\n**Agent 用法：**\n适合用于调试和记录 Lynx App DOM 的当前状态。它会强制从 CDP 服务器获取一份新的 `DOM.getDocument` 快照，并递归遍历该树，构建包含标签和属性的字符串（例如 `<view id=\"main\"><text>...</text></view>`）。"
  },
  "KittenLynxView.goto.summary": {
    "hash": "80ccaca7",
    "text": "将 Lynx App 导航到指定的 Lynx bundle URL，并附加到对应的 CDP 会话。\n\n**工作原理（Agent 必须了解）：**\n与标准 Web 浏览器不同，在 Lynx 中调用 `Page.navigate` 会创建一个**新的**调试会话，而不是复用当前会话。本方法通过以下步骤屏蔽这一复杂性：\n1. 等待 devtool 服务器启动。\n2. 发送 `App.openPage`（或兜底消息）以触发导航。\n3. 通过 ADB **轮询会话列表**，找到 URL 与目标 bundle URL 匹配的新会话。\n4. 自动重新附加到匹配的会话，并获取初始 DOM 树（`DOM.getDocument`）。"
  },
  "KittenLynxView.goto.params.url": {
    "hash": "85d8102c",
    "text": "要导航到的 Lynx bundle 的绝对 URL（例如 `'http://localhost:8080/dist/main.lynx.bundle'`）。"
  },
  "KittenLynxView.locator.summary": {
    "hash": "6eb9aa84",
    "text": "在当前页面中定位第一个匹配给定 CSS 选择器的 DOM 元素。\n\n**Agent 用法：**\n行为与 `document.querySelector` 或 Playwright 的 `page.locator()` 完全一致。它依赖 CDP 的 `DOM` 域。调用前务必确保 `goto()` 已成功完成，否则 DOM 树尚不存在。"
  },
  "KittenLynxView.locator.params.selector": {
    "hash": "d1dff093",
    "text": "指向目标元素的有效 CSS 选择器字符串（例如 `'view'`、`'#submit-btn'`、`'.container > text'`）。"
  },
  "KittenLynxView.onAttachedToTarget.summary": {
    "hash": "d75d5607",
    "text": "将 LynxView 附加到指定的 Lynx devtool 会话，并初始化 CDP 通道。\n\n**内部机制：**\n该方法为指定的 `sessionId` 创建一个 `CDPChannel`，并立即发出 `DOM.getDocument` 请求以缓存根文档节点。该请求必须成功，页面才能交互。\n\n**Agent 须知：**\n该方法通常视为内部方法，但并非 `private`。一般无需手动调用，因为 `goto()` 会自动处理会话附加。"
  },
  "KittenLynxView.onAttachedToTarget.params.sessionId": {
    "hash": "a73657de",
    "text": "要附加的 Lynx devtool 数字会话 ID，通过 `sendListSessionMessage` 获取。"
  },
  "KittenLynxView.screenshot.summary": {
    "hash": "d69cac2c",
    "text": "截取页面截图。"
  },
  "KittenLynxView.screenshot.params.options": {
    "hash": "c1476f7a",
    "text": "截图选项，例如路径、格式和质量。"
  },
  "KittenLynxView.url.summary": {
    "hash": "89cc2b69",
    "text": "返回最近一次通过 [`goto`](api:KittenLynxView.goto) 成功加载的 URL。"
  },
  "KittenLynxView.getKittenLynxViewById.summary": {
    "hash": "436b2f48",
    "text": "通过字符串形式的数字 ID 获取之前创建的 `KittenLynxView` 实例。\n\n**对 Agent 的用处：**\n主要用于交叉引用，或在不传递对象引用的情况下查找已有的视图。"
  },
  "KittenLynxView.getKittenLynxViewById.params.id": {
    "hash": "abf206a4",
    "text": "LynxView 数字 ID 的字符串形式（例如 `'1'`、`'2'`）。"
  },
  "Lynx.summary": {
    "hash": "3cfea410",
    "text": "kitten-lynx 测试框架的主入口。\n\n提供类似 Puppeteer 的 API，通过 ADB 和 Chrome DevTools Protocol 连接运行在 Android 设备（真机或模拟器）上的 Lynx 应用。"
  },
  "Lynx.example.0": {
    "hash": "877b018a",
    "text": "```typescript\nconst lynx = await Lynx.connect({ deviceId: 'localhost:5555' });\nconst page = await lynx.newPage();\nawait page.goto('http://example.com/bundle.lynx.bundle');\nconst content = await page.content();\nawait lynx.close();\n```"
  },
  "Lynx.close.summary": {
    "hash": "03ae3c88",
    "text": "关闭当前的 ADB/CDP 连接并释放相关资源。\n\n**Agent 用法：**\n请确保在测试的清理阶段（例如 `afterAll()` 中）调用该方法，以免残留的 Node.js 进程或挂起的 ADB 连接影响后续测试运行。"
  },
  "Lynx.newPage.summary": {
    "hash": "7d800e76",
    "text": "为已连接的 Lynx 环境创建一个新的页面对象。\n\n**Agent 用法：**\n类似于 Puppeteer 的 `browser.newPage()`。通过 `Lynx.connect()` 成功创建 `Lynx` 连接实例后，调用该方法获取一个 `KittenLynxView`，然后即可在该视图上调用 `post.goto(url)` 导航到 Lynx bundle。"
  },
  "Lynx.connect.summary": {
    "hash": "c042c029",
    "text": "主要的初始化方法。通过 ADB 连接 Lynx 应用的 devtool 服务器。\n\n**连接流程（Agent 指南）：**\n1. 发现目标 ADB 设备（Android 真机或模拟器）。\n2. 强制停止目标应用以确保干净的状态（`adb shell am force-stop`）。\n3. 在设备上启动应用（通常为 Lynx Explorer）。\n4. 向 `Connector` 查询可用的客户端，并按设备 ID 和包名进行匹配。\n5. 打开 devtool 总开关（`enable_devtool`）。\n\n**使用时机：**\n应在任何测试脚本或交互流程中最先调用该方法。"
  },
  "Lynx.connect.params.options": {
    "hash": "37e0c6c7",
    "text": "配置连接参数，例如 `appPackage` 和 `deviceId`（连接了多台设备时很有用）。"
  }
}
