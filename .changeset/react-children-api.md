---
"@lynx-js/react": minor
"@lynx-js/genui": patch
"@lynx-js/react-rsbuild-plugin": patch
---

Expose the `Children` API from ReactLynx and freeze the arrays returned by `Children.map`, `Children.forEach`, and `Children.toArray`.

Allow `@lynx-js/react` 0.121 and newer in GenUI peer dependency ranges.
