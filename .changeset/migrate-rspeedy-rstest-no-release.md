---

---

Migrate the rspeedy and webpack unit tests from Vitest to Rstest and remove the internal `@lynx-js/vitest-setup` package. Test-infrastructure only — no published package behavior changes, so no release is required.
