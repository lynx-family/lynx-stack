---
"@lynx-js/qrcode-rsbuild-plugin": patch
---

Run the interactive shortcuts through a single reader that follows the current dev server, so a restart hands a pending prompt over to the new server instead of opening a second prompt or swallowing the next key press.
