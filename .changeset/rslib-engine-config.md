---
"@lynx-js/rsbuild-plugin": minor
"@lynx-js/lynx-bundle-rslib-config": minor
---

Apply only the config of `pluginLynx` for the callers that drive the build themselves, so an external bundle resolves its filename from it. Its output is unchanged.
