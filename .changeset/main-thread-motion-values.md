---
"@lynx-js/react": patch
---

Add typed `MainThreadObject` handles whose lifecycle is defined by Main Thread Functions, with identity-preserving capture and hydration and type-scoped initial-payload inspection.
