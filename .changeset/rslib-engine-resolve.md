---
"@lynx-js/rsbuild-plugin": minor
---

Apply the Lynx module resolution to an external bundle as well, so it picks the `lynx` export condition, main field and `index.lynx` entry the way a template build does.
