---
"@lynx-js/web-elements": patch
"@lynx-js/web-explorer": patch
---

fix: list-item `contain` property changes from `size` to `layout paint`, because the size of the `list-item` can be expanded by its children.
