---
"@lynx-js/react": minor
---

Add `createPortal` support. Import it from `@lynx-js/react`; the `container` argument must be a ref obtained from a ReactLynx element. Refs from `lynx.createSelectorQuery()` or third-party sources are not supported as portal containers.
