---
"@lynx-js/template-webpack-plugin": minor
---

Add `customSectionNaming`, `appType` and `enableSectionBytecode`, so a caller can have a bundle assembled entirely from custom sections under names of its own. A `FetchBundle` lazy bundle is one such caller; an external bundle is another.
