---
'@lynx-js/react-rsbuild-plugin': patch
'@lynx-js/react-webpack-plugin': patch
'@lynx-js/template-webpack-plugin': patch
---

Add opt-in debug metadata emission for ReactLynx builds and expose template hooks so custom plugins can attach uploaded `debugMetadataUrl` and related metadata before encode.
