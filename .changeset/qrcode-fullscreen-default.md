---
"@lynx-js/qrcode-rsbuild-plugin": minor
---

`fullscreen: true` now shows the fullscreen URL as the default QR code instead of appending it to the schema rotation; press `a` to switch to the other schemas.
