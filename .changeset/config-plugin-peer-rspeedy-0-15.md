---
"@lynx-js/config-rsbuild-plugin": patch
---

Widen the `@lynx-js/rspeedy` peer dependency range to include `^0.15.0`, so `@lynx-js/config-rsbuild-plugin` stays compatible with the rspeedy minor bump that ships the `output.filename.bundle` function form.
