---
"@lynx-js/web-elements": patch
---

Make the internal `x-input` input and form parts inherit `box-sizing` from the
host element, keeping sizing behavior consistent with inherited dimensions and
borders.
