---
"@lynx-js/react-compat": minor
---

Add `@lynx-js/react-compat`, a React-versioned alias of `@lynx-js/react/compat`. Install it as `"react": "npm:@lynx-js/react-compat"` to satisfy the `react` peer dependency of React libraries under strict package managers.
