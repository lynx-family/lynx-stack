---
"@lynx-js/rsbuild-plugin": patch
---

Print the dev and preview server URLs from each environment's own entries and `dev.assetPrefix`, instead of crossing the root `source.entry`/`dev.assetPrefix` with every environment.
