---
"@lynx-js/qrcode-rsbuild-plugin": minor
---

Support [Rsbuild v2](https://rsbuild.rs/guide/upgrade/v1-to-v2#plugin-api) and rename `api.onAfterStartProdServer` hook to `api.onAfterStartPreviewServer` and `api.onDevCompileDone` hook to `api.onAfterDevCompile`.
