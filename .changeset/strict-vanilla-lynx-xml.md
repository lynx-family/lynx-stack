---
"@lynx-js/web-core": minor
---

Parse single-file Lynx XML with the current Vanilla Lynx `engine-version` and
`thread="main"` / `thread="background"` syntax, and reject the legacy wrapper
syntax.
