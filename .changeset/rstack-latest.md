---
"@lynx-js/rspeedy": patch
---

Update the Rstack toolchain: Rspack 2.2.2, Rsbuild 2.2.3, Rslib 1.0.0 and Rstest 0.11.12. `@lynx-js/rspeedy` is the only published package whose manifest changes, since it is the only one that carries a catalog entry (`@rsbuild/core`) in `dependencies`.
