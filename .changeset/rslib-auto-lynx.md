---
"@lynx-js/react-rsbuild-plugin": minor
---

Apply the Lynx build engine for the `rslib` caller as well, so an external bundle gets the engine config and the plugins that tap the template hooks without `pluginLynx` being added by hand.
