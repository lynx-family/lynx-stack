---

---

No package release is required because this change only moves shared runtime internals for Element Template initData support, wires existing data-change flush options through a shared commit context, and updates internal runtime tests without changing release-facing defaults.
