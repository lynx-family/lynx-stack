---
'@lynx-js/web-elements': patch
---

Preserve `blur-radius` length units on Web, including reactive `rpx` and `ppx` values, instead of interpreting all radii as pixels. Clear the blur for invalid or removed radii.
