---
"@lynx-js/autolink-codegen": minor
---

Generate mobile and desktop registration from one weak-node-api implementation,
expose pure Node-API backends through `NativeModules`, and preserve existing
AutoLink registrations before falling back to runtime loaders.
