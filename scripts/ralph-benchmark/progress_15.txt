# Ralph Phase 1.5 Benchmark Progress

Branch: Huxpro/lynx-dom-shim-benchmark
PRD: /Users/bytedance/github/lynx-stack/.worktrees/lynx-dom-shim-benchmark/Phase_1_5_PRD.md
Completion promise: PHASE_1_5_DONE

## Story Status

- US-151: DONE — aggregate token cost into report
- US-152: TODO — wire puppeteer to enable M4 visual scoring
- US-153: TODO — tighten mock PAPI / mock Shim to reject unknown tags
- US-154: TODO — multi-model support + cost estimator
- US-155: TODO — n=50 full sweep + decision document (HARD STOP gate before sweep)

## Notes for next iteration

Always use absolute paths in Bash calls (cwd does not persist).
Worktree: /Users/bytedance/github/lynx-stack/.worktrees/lynx-dom-shim-benchmark/

**Environment quirk (carry-over from Phase 1):**
Always prepend before any pnpm/node/tsc command:
  export PATH="/Users/bytedance/.nvm/versions/node/v22.19.0/bin:$PATH"
  export NODE_EXTRA_CA_CERTS=/tmp/all-cas.pem

**Lint contract (from Phase 1 progress.txt — do not relearn):**
- ESLint forbids process.exit() → throw new Error()
- Biome forbids console.log → console.info
- TS strictest + verbatimModuleSyntax + isolatedDeclarations on
- n/file-extension-in-import: 'always' overridden for packages/dom-shim/**/*.ts
- Biome useAwait: no async without await; use Promise.resolve/reject for sync paths
- ESLint unicorn/consistent-function-scoping: hoist noop to module scope

## Completed iterations log

### Iteration 1 — US-151 (token cost aggregation) DONE
- Read PRD.md, REPORT.md, Phase_1_5_PRD.md in full
- Added benchmarks/src/utils/pricing.ts with MODEL_PRICING (gpt-4o, gpt-4o-mini, claude-opus-4-7, claude-sonnet-4-6) and estimateUsdCost/hasPricing helpers
- Extended RouteMetrics with total_input_tokens, total_output_tokens, estimated_cost_usd, mean_tokens_per_prompt, optional pricing_missing flag
- harness.ts computeMetrics now sums tokens across all rounds per (prompt, route) cell; aggregate threads model_id through
- Markdown summary + per-category tables gained tokens (in/out) and cost (USD) columns; per-prompt detail table gained cost (USD) (sum across routes for that prompt); footnote surfaces pricing_missing if model not in table
- schema/result.schema.json RouteMetrics accepts the new optional fields
- Verification:
  * pnpm typecheck — clean
  * pnpm test:schema-validate — positive + 6 negative cases all pass
  * eslint + biome on changed files — clean (after switching `m && m.pricing_missing` to `m?.pricing_missing`)
  * dry-run smoke (--routes A,B,C --prompts P001,P002 --rounds 1) — exit 0, report.md well-formed with new columns
  * SPOT_CHECK_HARD_N10.json verification: per-route token totals come out to
      Route A: input=37,864  output=4,984  cost=$0.1445  (highest input as predicted, 6.6× B)
      Route B: input= 5,754  output=5,095  cost=$0.0653
      Route C: input=18,885  output=7,351  cost=$0.1207
    Sensible — A is much higher than B/C because it embeds the 476-line .d.ts per system prompt
- Lint gotchas this iteration: biome useOptionalChain on `m && m.pricing_missing` → `m?.pricing_missing`
- Commit pending
