# Ralph Phase 1.5 Benchmark Progress

Branch: Huxpro/lynx-dom-shim-benchmark
PRD: /Users/bytedance/github/lynx-stack/.worktrees/lynx-dom-shim-benchmark/Phase_1_5_PRD.md
Completion promise: PHASE_1_5_DONE

## Story Status

- US-151: DONE — aggregate token cost into report
- US-152: PARTIAL (code) — wire puppeteer to enable M4 visual scoring
  Code complete: puppeteer dep added, rasterize.ts created, harness wired to rasterize HTML → PNG before scoring, browser shutdown hook in place.
  Verified: dry-run smoke clean; manual rasterizePreview() smoke against /tmp/test-preview.html produced a 7,922-byte PNG using system Chrome (no Chromium download).
  BLOCKED on AC final bullet: re-running hard n=10 spot check requires OPENAI_API_KEY in env (also needed for US-153 re-run, US-154 verify, US-155 sweep). Both keys are NOT present in current session env — the boot prompt's "already in session env from a prior turn" assumption did not hold.
- US-153: PARTIAL (code) — tighten mock PAPI / mock Shim to reject unknown tags
  Code complete: ALLOWED_LYNX_TAGS exported from mock-papi; __CreateElement throws on unknown tag; mock-shim createMock wraps that as a DOM-style "Failed to execute 'createElement' on 'Document'" error; route-c-schema.json tag enum expanded to match.
  Verified manual smoke: __CreateElement('view') ok, __CreateElement('div') throws "Unknown Lynx tag: div"; document.createElement('view') ok, document.createElement('div') throws the DOM-style error. Typecheck + dry-run + schema-validate + lint all clean.
  BLOCKED on AC final bullet: re-running hard n=10 → SPOT_CHECK_HARD_N10_V3.{md,json} requires API keys (same blocker as US-152).
- US-154: DONE — multi-model support + cost estimator
  Added estimateInputTokens(system, user) (chars/4 heuristic) to anthropic-client.ts.
  Exported buildSystemPrompt() from route-a-papi and SYSTEM_PROMPT consts from route-b and route-c so the CLI can size each route's prompt without firing an LLM call.
  cli.ts: --models gpt-4o,claude-opus-4-7 (multi-model; overrides --model); --estimate-only walks corpus + routes and prints `Estimated cost for <model>: $mid (range $lo..$hi)` per model and a totals line, exit 0 without any LLM call. Multi-model run writes report.{md,json} under out_dir/<model_id>/ + a cross_model_summary.md side-by-side table.
  Verified:
    * pnpm typecheck — clean
    * eslint + biome on changed files — clean (after --fix reordered imports + dropped a redundant cast)
    * dry-run smoke — exit 0
    * `--estimate-only --all --routes A,B,C --rounds 3 --models gpt-4o,claude-opus-4-7` prints:
        Estimated cost for gpt-4o: $6.34 (range $3.64..$9.04)
        Estimated cost for claude-opus-4-7: $44.14 (range $23.89..$64.39)
        Estimated cost: $50.49 (range $27.54..$73.44)
      Inside PRD §5.2's $20..$60 envelope.
- US-155: TODO — n=50 full sweep + decision document (HARD STOP gate before sweep)

## Notes for next iteration

Always use absolute paths in Bash calls (cwd does not persist).
Worktree: /Users/bytedance/github/lynx-stack/.worktrees/lynx-dom-shim-benchmark/

**Environment quirk (carry-over from Phase 1):**
Always prepend before any pnpm/node/tsc command:
  export PATH="/Users/bytedance/.nvm/versions/node/v22.19.0/bin:$PATH"
  export NODE_EXTRA_CA_CERTS=/tmp/all-cas.pem

**Lint contract (from Phase 1 progress.txt — do not relearn):**
- ESLint forbids process.exit() → throw new Error()
- Biome forbids console.log → console.info
- TS strictest + verbatimModuleSyntax + isolatedDeclarations on
- n/file-extension-in-import: 'always' overridden for packages/dom-shim/**/*.ts
- Biome useAwait: no async without await; use Promise.resolve/reject for sync paths
- ESLint unicorn/consistent-function-scoping: hoist noop to module scope

## Completed iterations log

### Iteration 1 — US-151 (token cost aggregation) DONE
- Read PRD.md, REPORT.md, Phase_1_5_PRD.md in full
- Added benchmarks/src/utils/pricing.ts with MODEL_PRICING (gpt-4o, gpt-4o-mini, claude-opus-4-7, claude-sonnet-4-6) and estimateUsdCost/hasPricing helpers
- Extended RouteMetrics with total_input_tokens, total_output_tokens, estimated_cost_usd, mean_tokens_per_prompt, optional pricing_missing flag
- harness.ts computeMetrics now sums tokens across all rounds per (prompt, route) cell; aggregate threads model_id through
- Markdown summary + per-category tables gained tokens (in/out) and cost (USD) columns; per-prompt detail table gained cost (USD) (sum across routes for that prompt); footnote surfaces pricing_missing if model not in table
- schema/result.schema.json RouteMetrics accepts the new optional fields
- Verification:
  * pnpm typecheck — clean
  * pnpm test:schema-validate — positive + 6 negative cases all pass
  * eslint + biome on changed files — clean (after switching `m && m.pricing_missing` to `m?.pricing_missing`)
  * dry-run smoke (--routes A,B,C --prompts P001,P002 --rounds 1) — exit 0, report.md well-formed with new columns
  * SPOT_CHECK_HARD_N10.json verification: per-route token totals come out to
      Route A: input=37,864  output=4,984  cost=$0.1445  (highest input as predicted, 6.6× B)
      Route B: input= 5,754  output=5,095  cost=$0.0653
      Route C: input=18,885  output=7,351  cost=$0.1207
    Sensible — A is much higher than B/C because it embeds the 476-line .d.ts per system prompt
- Lint gotchas this iteration: biome useOptionalChain on `m && m.pricing_missing` → `m?.pricing_missing`
- Commit pending

### Iteration 1 (cont.) — US-152 code wiring + puppeteer install plumbing
- Added puppeteer ^23.11.1 to packages/dom-shim devDependencies
- pnpm 11 requires build-script approval; set `puppeteer: false` under `allowBuilds:` in pnpm-workspace.yaml (replaces the auto-generated placeholder). Chromium download is skipped intentionally because we use system Chrome.
- Created benchmarks/src/scoring/rasterize.ts: lazy puppeteer browser singleton. resolveExecutablePath() honors PUPPETEER_EXECUTABLE_PATH then falls back to /Applications/Google Chrome.app/Contents/MacOS/Google Chrome on darwin. Default viewport 800×1200 per RUBRIC.md M4. process.on('exit') hook + explicit closeBrowser() called from harness finally block.
- harness.ts: when route returns render_ok=true and screenshot_path ends with .html, rasterize to .png, point screenshot_path at the PNG; on failure append `rasterize failed: <msg>` to error_log and set screenshot_path=null. visual scorer then fires with a real PNG.
- Verification:
  * pnpm install — exit 0 (with `puppeteer: false` in allowBuilds, no more ERR_PNPM_IGNORED_BUILDS)
  * pnpm typecheck — clean
  * eslint + biome on changed files — clean
  * dry-run smoke (--routes A,B,C --prompts P001,P002 --rounds 1) — exit 0
  * Manual rasterizePreview smoke against a 2-paragraph HTML wrote a 7,922-byte PNG using system Chrome (no network)
- BLOCKER (surfaced to user this iteration): paid n=10 re-run cannot proceed without OPENAI_API_KEY in env. Same blocker also covers US-153 paid re-run, US-154 verify, and US-155 sweep. Loop will park here until keys are exported.

## Notes for next iteration

If OPENAI_API_KEY is now exported when the next iteration starts:
1. Re-run hard n=10 with puppeteer enabled:
   `pnpm -F @lynx-js/dom-shim benchmark --routes A,B,C --prompts P007,P014,P015,P023,P029,P030,P034,P041,P048,P049 --rounds 3 --model gpt-4o --out packages/dom-shim/benchmarks/reports/spot-check-v2`
2. Copy report.{md,json} to packages/dom-shim/SPOT_CHECK_HARD_N10_V2.{md,json}
3. Verify visual_score is numeric for each render_ok=true record (the AC for US-152)
4. Commit `feat(dom-shim): US-152 — wire puppeteer rasterization for M4 visual scoring`
5. Proceed to US-153.

If keys still absent:
- Do not write more code.
- Print a one-paragraph blocker reminder and end the iteration without emitting the promise.
