{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "@lynx-js/react-runtime#build"
      ],
      "inputs": [
        "src/**",
        "scripts/build.mjs",
        "rslib.config.ts"
      ],
      "outputs": [
        "dist",
        "lib"
      ]
    }
  }
}
