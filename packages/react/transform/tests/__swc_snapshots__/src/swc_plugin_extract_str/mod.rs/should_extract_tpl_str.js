var _EXTRACT_STR = [
    "Hello",
    "world",
    "middle"
];
const greeting = `${_EXTRACT_STR[0]} ${name}`;
const suffix = `${value} ${_EXTRACT_STR[1]}`;
const wrapped = `${left} ${_EXTRACT_STR[2]} ${right}`;
const spaces = ` ${content} `;
