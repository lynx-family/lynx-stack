{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src/**",
        "package.json",
        "rslib.config.ts",
        "tsconfig.json"
      ],
      "outputs": [
        "dist/**"
      ]
    }
  }
}
