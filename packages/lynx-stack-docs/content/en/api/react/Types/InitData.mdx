# Interface: InitData

The interface you can extends so that the `defaultDataProcessor` returning value can be customized

Should be used with `lynx.registerDataProcessors`. See more examples at [Lynx.registerDataProcessors](Lynx.mdx#registerdataprocessors).
