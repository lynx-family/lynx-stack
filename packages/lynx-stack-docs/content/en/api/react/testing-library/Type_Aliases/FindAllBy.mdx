# Type Alias: FindAllBy\<Arguments *extends* `any`[]\>

```ts
type FindAllBy<Arguments extends any[]> = QueryMethod<[Arguments[0], Arguments[1]?, waitForOptions?], Promise<HTMLElement[]>>;
```

## Type Parameters

| Type Parameter |
| ------ |
| `Arguments` *extends* `any`[] |
