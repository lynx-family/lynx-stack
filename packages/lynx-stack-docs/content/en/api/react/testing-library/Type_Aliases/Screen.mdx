# Type Alias: Screen\<Q *extends* [`Queries`](../Interfaces/Queries.mdx) = *typeof* [`queries`](../Namespaces/queries.mdx)\>

```ts
type Screen<Q extends Queries = typeof queries> = BoundFunctions<Q> & object;
```

## Type Declaration

### debug

```ts
debug: (element?: (Element | HTMLDocument)[] | Element | HTMLDocument, maxLength?: number, options?: OptionsReceived) => void;
```

Convenience function for `pretty-dom` which also allows an array
of elements

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element?` | (`Element` \| `HTMLDocument`)[] \| `Element` \| `HTMLDocument` |
| `maxLength?` | `number` |
| `options?` | [`OptionsReceived`](prettyFormat.OptionsReceived.mdx) |

#### Returns

`void`

### logTestingPlaygroundURL

```ts
logTestingPlaygroundURL: (element?: Element | HTMLDocument) => string;
```

Convenience function for `Testing Playground` which logs and returns the URL that
can be opened in a browser

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element?` | `Element` \| `HTMLDocument` |

#### Returns

`string`

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Q` *extends* [`Queries`](../Interfaces/Queries.mdx) | *typeof* [`queries`](../Namespaces/queries.mdx) |
