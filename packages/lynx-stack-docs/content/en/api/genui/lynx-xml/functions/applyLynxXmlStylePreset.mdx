# Function: applyLynxXmlStylePreset()

```ts
function applyLynxXmlStylePreset(source: string, stylePreset?: "default"): string;
```

Inject preset CSS into a direct Element PAPI document without changing scripts.

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `source` | `string` | `undefined` |
| `stylePreset` | `"default"` | `'default'` |

## Returns

`string`
