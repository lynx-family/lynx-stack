# Function: generateMainThreadScript()

```ts
function generateMainThreadScript(xmlFragment: string): string;
```

Convert an XML element fragment into Element PAPI calls for renderPage().

## Parameters

| Parameter | Type |
| ------ | ------ |
| `xmlFragment` | `string` |

## Returns

`string`
