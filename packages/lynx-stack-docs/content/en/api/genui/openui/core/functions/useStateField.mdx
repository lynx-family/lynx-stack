# Function: useStateField()

```ts
function useStateField<T = unknown>(name: string, value?: T): StateField<InferStateFieldValue<T>>;
```

Resolve a named OpenUI state field against form state and reactive bindings.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `value?` | `T` |

## Returns

[`StateField`](../interfaces/StateField.mdx)\<`InferStateFieldValue`\<`T`\>\>
