# Interface: EvaluationContext

## Properties

### extraScope?

```ts
optional extraScope?: Record<string, unknown>;
```

Extra scope for $value injection during reactive prop evaluation

## Methods

### getState()

```ts
getState(name: string): unknown;
```

Read $variable from the store

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |

#### Returns

`unknown`

***

### resolveRef()

```ts
resolveRef(name: string): unknown;
```

Resolve a reference to another declaration's evaluated value

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |

#### Returns

`unknown`
