# Function: useAction()

```ts
function useAction(props: ActionProps): object;
```

Create a `sendAction` callback that resolves dynamic action payload values
and dispatches user events or function calls through the current processor.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`ActionProps`](../interfaces/ActionProps.mdx) |

## Returns

`object`

### sendAction

```ts
sendAction: (action: 
  | {
  event: {
     context?: Record<string, 
        | string
        | number
        | boolean
        | any[]
        | {
        path: string;
      }
        | {
        args: Record<string, any>;
        call: string;
        returnType: "string" | "number" | "boolean" | "object" | "array" | "void" | "any";
     }>;
     name: string;
  };
}
  | {
  functionCall: {
     args: Record<string, any>;
     call: string;
     returnType: "string" | "number" | "boolean" | "object" | "array" | "void" | "any";
  };
}) => Promise<unknown>;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `action` | \| \{ `event`: \{ `context?`: `Record`\<`string`, \| `string` \| `number` \| `boolean` \| `any`[] \| \{ `path`: `string`; \} \| \{ `args`: `Record`\<`string`, `any`\>; `call`: `string`; `returnType`: `"string"` \| `"number"` \| `"boolean"` \| `"object"` \| `"array"` \| `"void"` \| `"any"`; \}\>; `name`: `string`; \}; \} \| \{ `functionCall`: \{ `args`: `Record`\<`string`, `any`\>; `call`: `string`; `returnType`: `"string"` \| `"number"` \| `"boolean"` \| `"object"` \| `"array"` \| `"void"` \| `"any"`; \}; \} |

#### Returns

`Promise`\<`unknown`\>
