# Function: Slider()

```ts
function Slider(props: SliderProps): ReactNode;
```

Render a numeric slider bound to the surface data model.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `SliderProps` |

## Returns

`ReactNode`
