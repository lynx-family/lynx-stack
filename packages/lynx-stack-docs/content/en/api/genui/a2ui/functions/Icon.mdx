# Function: Icon()

```ts
function Icon(props: IconProps): ReactNode;
```

Render a named icon from the catalog's supported icon set.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `IconProps` |

## Returns

`ReactNode`
