# Function: Column()

```ts
function Column(props: ColumnProps): ReactNode;
```

Render child component ids in vertical order.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `ColumnProps` |

## Returns

`ReactNode`
