# Function: Divider()

```ts
function Divider(props: DividerProps): ReactNode;
```

Render a horizontal or vertical visual separator.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `DividerProps` |

## Returns

`ReactNode`
