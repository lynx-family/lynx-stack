# Function: PieChart()

```ts
function PieChart(props: PieChartProps): ReactNode;
```

Render numeric slices as a pie or donut chart.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `PieChartProps` |

## Returns

`ReactNode`
