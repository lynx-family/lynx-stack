# Function: Card()

```ts
function Card(props: CardProps): ReactNode;
```

Render a single-child card container.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | `CardProps` |

## Returns

`ReactNode`
