# Type Alias: A2UIClientEventMessage

```ts
type A2UIClientEventMessage = 
  | string
  | {
  sessionId?: string;
  text?: string;
}
  | {
  sessionId?: string;
  userAction: UserActionPayload;
};
```
