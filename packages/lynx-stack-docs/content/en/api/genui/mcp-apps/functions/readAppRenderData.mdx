# Function: readAppRenderData()

```ts
function readAppRenderData(value: unknown): AppRenderData<unknown> | null;
```

Parses and validates the renderer-independent portion of MCP Apps data.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |

## Returns

[`AppRenderData`](../interfaces/AppRenderData.mdx)\<`unknown`\> \| `null`
