# Function: clamp()

```ts
function clamp(
   min: number, 
   max: number, 
   v: number
): number;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `min` | `number` |
| `max` | `number` |
| `v` | `number` |

## Returns

`number`
