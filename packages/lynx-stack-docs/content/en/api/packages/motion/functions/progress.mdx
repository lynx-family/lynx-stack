# Function: progress()

```ts
function progress(
   from: number, 
   to: number, 
   value: number
): number;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `from` | `number` |
| `to` | `number` |
| `value` | `number` |

## Returns

`number`
