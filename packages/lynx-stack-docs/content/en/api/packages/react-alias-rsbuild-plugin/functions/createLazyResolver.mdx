# Function: createLazyResolver()

```ts
function createLazyResolver(
   rspack: Rspack, 
   directory: string, 
   conditionNames: string[]
): (request: string) => Promise<string>;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `rspack` | `Rspack` |
| `directory` | `string` |
| `conditionNames` | `string`[] |

## Returns

(`request`: `string`) => `Promise`\<`string`\>
