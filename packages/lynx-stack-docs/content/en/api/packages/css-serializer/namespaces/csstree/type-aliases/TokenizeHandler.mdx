# Type Alias: TokenizeHandler

```ts
type TokenizeHandler = (source: string, onToken: (type: number, start: number, end: number) => void) => void;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `source` | `string` |
| `onToken` | (`type`: `number`, `start`: `number`, `end`: `number`) => `void` |

## Returns

`void`
