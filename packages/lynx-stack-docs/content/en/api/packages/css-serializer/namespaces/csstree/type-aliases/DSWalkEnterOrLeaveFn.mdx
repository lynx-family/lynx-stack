# Type Alias: DSWalkEnterOrLeaveFn

```ts
type DSWalkEnterOrLeaveFn = (node: DSNode) => void;
```

Definition syntax walk callback

## Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | [`DSNode`](DSNode.mdx) |

## Returns

`void`
