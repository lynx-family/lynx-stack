# Function: parse()

```ts
function parse(content: string, options?: object): object;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `content` | `string` |
| `options` | \{ `filename?`: `string`; `plugins?`: [`Plugin`](../interfaces/Plugin.mdx)[]; `projectRoot?`: `string`; \} |
| `options.filename?` | `string` |
| `options.plugins?` | [`Plugin`](../interfaces/Plugin.mdx)[] |
| `options.projectRoot?` | `string` |

## Returns

`object`

### errors

```ts
errors: ParserError[];
```

### root

```ts
root: LynxStyleNode[];
```
