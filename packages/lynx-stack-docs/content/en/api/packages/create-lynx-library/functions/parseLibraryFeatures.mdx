# Function: parseLibraryFeatures()

```ts
function parseLibraryFeatures(source: string): LibraryFeature[];
```

Parses a comma-separated library feature list from CLI input.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `source` | `string` |

## Returns

[`LibraryFeature`](../type-aliases/LibraryFeature.mdx)[]
