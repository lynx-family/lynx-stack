# Class: ReactRefreshRspackPlugin

ReactRefreshRspackPlugin allows using fast refresh in ReactLynx.

## Example

```js
// rspack.config.js
import { ReactRefreshRspackPlugin } from '@lynx-js/react-refresh-webpack-plugin'
export default {
  module: {
    rules: [
      {
        test: /\.[jt]sx?$/,
        exclude: [
          /node_modules/,
          /@lynx-js/,
          /react-refresh/,
          /compiler-nodiff-runtime3/,
          ReactRefreshRspackPlugin.loader,
        ],
        use: [ReactRefreshRspackPlugin.loader],
      },
    ],
  },
  plugins: [new ReactRefreshRspackPlugin()],
}
```

## Constructors

### Constructor

```ts
new ReactRefreshRspackPlugin(options?: ReactRefreshRspackPluginOptions): ReactRefreshRspackPlugin;
```

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `options` | [`ReactRefreshRspackPluginOptions`](../interfaces/ReactRefreshRspackPluginOptions.mdx) | The options for react refresh. |

#### Returns

`ReactRefreshRspackPlugin`

## Properties

### loader

```ts
static loader: string;
```

The loader for react refresh.

#### Remarks

Please note that the runtime of react should be ignored. See the example below:

#### Example

```js
// rspack.config.js
import { ReactRefreshRspackPlugin } from '@lynx-js/react-refresh-webpack-plugin'
import { LAYERS } from '@lynx-js/react-webpack-plugin'

export default {
  module: {
    rules: [
      {
        issuerLayer: LAYERS.BACKGROUND,
        test: /\.[jt]sx?$/,
        exclude: [
          /node_modules/,
          /@lynx-js/react/,
          ReactRefreshRspackPlugin.loader,
        ],
        use: [ReactRefreshRspackPlugin.loader],
      },
    ],
  },
  plugins: [new ReactRefreshRspackPlugin()],
}
```

## Methods

### apply()

```ts
apply(compiler: Compiler): void;
```

The entry point of a rspack plugin.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `compiler` | `Compiler` | the rspack compiler |

#### Returns

`void`
