# Type Alias: CustomizedSchemaFn

```ts
type CustomizedSchemaFn = (url: string) => string | Record<string, string>;
```

Customize the generated schema.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `url` | `string` |

## Returns

`string` \| `Record`\<`string`, `string`\>
