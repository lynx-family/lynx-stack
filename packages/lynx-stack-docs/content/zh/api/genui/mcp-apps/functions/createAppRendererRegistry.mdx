# 函数: createAppRendererRegistry()

```ts
function createAppRendererRegistry(renderers: readonly AppRenderer[]): AppRendererRegistry;
```

为本地提供的渲染器创建一个不可变的查找接口。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `renderers` | readonly [`AppRenderer`](../interfaces/AppRenderer.mdx)[] |

## 返回

[`AppRendererRegistry`](../interfaces/AppRendererRegistry.mdx)
