# 函数: readAppMarkdown()

```ts
function readAppMarkdown(value: unknown): string;
```

从 MCP Apps 宿主数据中读取作为回退的 Markdown 内容。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `value` | `unknown` |

## 返回

`string`
