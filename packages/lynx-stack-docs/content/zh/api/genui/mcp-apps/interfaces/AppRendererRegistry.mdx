# 接口: AppRendererRegistry

与协议无关的 MCP Apps 宿主使用的本地渲染器查找表。

## 属性

### getRenderer

```ts
getRenderer: (id: string) => AppRenderer | null;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `id` | `string` |

#### 返回

[`AppRenderer`](AppRenderer.mdx) \| `null`

***

### resolveRenderData

```ts
resolveRenderData: (value: unknown) => ResolvedAppRenderData | null;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `value` | `unknown` |

#### 返回

[`ResolvedAppRenderData`](ResolvedAppRenderData.mdx) \| `null`
