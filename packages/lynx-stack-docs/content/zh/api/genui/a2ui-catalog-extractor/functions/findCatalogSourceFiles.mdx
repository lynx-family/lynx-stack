# 函数: findCatalogSourceFiles()

```ts
function findCatalogSourceFiles(inputPath: string): string[];
```

在文件或目录路径下查找受支持的源文件。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `inputPath` | `string` |

## 返回

`string`[]
