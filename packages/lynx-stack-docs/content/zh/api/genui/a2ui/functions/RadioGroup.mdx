# 函数: RadioGroup()

```ts
function RadioGroup(props: RadioGroupComponentProps): ReactNode;
```

渲染一个绑定到 surface 数据模型的单选按钮组。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `props` | `RadioGroupComponentProps` |

## 返回

`ReactNode`
