# 函数: createTextCardMessages()

```ts
function createTextCardMessages(text: string): ServerToClientMessage[];
```

基于纯文本字符串构建用 Card 包裹 Text 的兜底消息流。在 agent 输出 `kind: 'text'` 的文本数据时使用。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `text` | `string` |

## 返回

[`ServerToClientMessage`](../type-aliases/ServerToClientMessage.mdx)[]
