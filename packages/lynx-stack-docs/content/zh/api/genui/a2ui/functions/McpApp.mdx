# 函数: McpApp()

```ts
function McpApp(props: McpAppProps): ReactNode;
```

在嵌套的 Lynx frame 中渲染受信任的 MCP App Lynx bundle。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `props` | `McpAppProps` |

## 返回

`ReactNode`
