# 接口: CheckLike

v0.9 的 `CheckRule` 形如 `{ condition, message }`，其中 `condition` 可以是布尔值、`DataBinding` 或 `FunctionCall`。这里接受宽松的 `unknown` 类型，这样组件 props 就不必为了透传而导入 v0_9 的类型。

## 属性

### condition

```ts
condition: unknown;
```

***

### message

```ts
message: string;
```
