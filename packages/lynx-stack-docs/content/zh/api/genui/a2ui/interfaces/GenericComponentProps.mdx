# 接口: GenericComponentProps

渲染器传给每个 catalog 组件的通用运行时 props。

## 可索引

```ts
[key: string]: unknown
```

## 属性

### dataContextPath?

```ts
optional dataContextPath?: string;
```

***

### id?

```ts
optional id?: string;
```

***

### sendAction?

```ts
optional sendAction?: (action: Record<string, unknown>) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `action` | `Record`\<`string`, `unknown`\> |

#### 返回

`void`

***

### setValue?

```ts
optional setValue?: (key: string, value: unknown) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `key` | `string` |
| `value` | `unknown` |

#### 返回

`void`

***

### surface

```ts
surface: Surface;
```
