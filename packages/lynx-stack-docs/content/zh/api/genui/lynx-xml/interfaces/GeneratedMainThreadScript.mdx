# 接口: GeneratedMainThreadScript

## 属性

### bindings

```ts
bindings: Record<string, string>;
```

运行时的 nodeMap 只保留显式声明的 XML id。

***

### classNames

```ts
classNames: string[];
```

可用于生成可选预设样式表的字面量模板类名。

***

### javascript

```ts
javascript: string;
```
