# 类型别名: CreateExplicitOpenUiLibraryOptions

```ts
type CreateExplicitOpenUiLibraryOptions = Omit<CreateOpenUiLibraryOptions, "includeDefaultComponents"> & object;
```

只由显式选择的组件组成的 Library 的选项。

## 类型声明

### includeDefaultComponents

```ts
includeDefaultComponents: false;
```
