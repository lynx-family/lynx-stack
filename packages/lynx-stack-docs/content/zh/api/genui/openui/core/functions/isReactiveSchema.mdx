# 函数: isReactiveSchema()

```ts
function isReactiveSchema(schema: unknown): boolean;
```

检查某个 schema 是否被标记为 reactive。供 Zod introspection 用于 $binding\<\> 前缀。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `schema` | `unknown` |

## 返回

`boolean`
