# 接口: PromptSpec

## 继承

- `BaseSpec`

## 属性

### additionalRules?

```ts
optional additionalRules?: string[];
```

***

### bindings?

```ts
optional bindings?: boolean;
```

启用 $variables、@Set、@Reset 和交互式筛选。默认：启用 toolCalls 时为 true。

***

### componentGroups?

```ts
optional componentGroups?: ComponentGroup$1[];
```

#### 继承自

```ts
BaseSpec.componentGroups
```

***

### components

```ts
components: Record<string, ComponentPromptSpec>;
```

#### 继承自

```ts
BaseSpec.components
```

***

### editMode?

```ts
optional editMode?: boolean;
```

***

### examples?

```ts
optional examples?: string[];
```

通用示例（静态 / 布局类模式）。`examples` 和 `toolExamples` 同时存在时都会包含。

***

### id?

```ts
optional id?: string;
```

#### 继承自

```ts
BaseSpec.id
```

***

### inlineMode?

```ts
optional inlineMode?: boolean;
```

***

### preamble?

```ts
optional preamble?: string;
```

***

### root?

```ts
optional root?: string;
```

#### 继承自

```ts
BaseSpec.root
```

***

### toolCalls?

```ts
optional toolCalls?: boolean;
```

启用 Query()、Mutation()、@Run 和工具工作流。默认：提供了工具时为 true。

***

### toolExamples?

```ts
optional toolExamples?: string[];
```

与工具相关的示例（Query/Mutation 模式）。`examples` 和 `toolExamples` 同时存在时都会包含。

***

### tools?

```ts
optional tools?: (string | ToolSpec)[];
```
