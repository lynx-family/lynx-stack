# 函数: tagSchemaId()

```ts
function tagSchemaId(schema: object, id: string): void;
```

为 schema 标记一个 ID，用于提示词中的签名。适用于需要友好类型名的非组件 schema（例如 ActionExpression）。只影响提示词输出，不影响 JSON Schema 的 $defs。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `schema` | `object` |
| `id` | `string` |

## 返回

`void`
