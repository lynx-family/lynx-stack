# 接口: FormValidationContextValue

OpenUI 表单字段的校验状态和辅助函数。

## 属性

### clearFieldError

```ts
clearFieldError: (name: string) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |

#### 返回

`void`

***

### errors

```ts
errors: Record<string, string | undefined>;
```

***

### getFieldError

```ts
getFieldError: (name: string) => string | undefined;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |

#### 返回

`string` \| `undefined`

***

### registerField

```ts
registerField: (name: string, rules: ParsedRule[], getValue: () => unknown) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `rules` | [`ParsedRule`](ParsedRule.mdx)[] |
| `getValue` | () => `unknown` |

#### 返回

`void`

***

### unregisterField

```ts
unregisterField: (name: string) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |

#### 返回

`void`

***

### validateField

```ts
validateField: (name: string, value: unknown, rules: ParsedRule[]) => boolean;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `value` | `unknown` |
| `rules` | [`ParsedRule`](ParsedRule.mdx)[] |

#### 返回

`boolean`

***

### validateForm

```ts
validateForm: () => boolean;
```

#### 返回

`boolean`
