# 类型别名: DefinedComponent\<T *extends* `$ZodObject` = `$ZodObject`\>

```ts
type DefinedComponent<T extends $ZodObject = $ZodObject> = CoreDefinedComponent<T, ComponentRenderer<z.infer<T>>>;
```

## 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `T` *extends* `$ZodObject` | `$ZodObject` |
