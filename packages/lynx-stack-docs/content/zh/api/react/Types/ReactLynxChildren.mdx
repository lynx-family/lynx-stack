# 类型别名: ReactLynxChildren

```ts
type ReactLynxChildren = Omit<typeof PreactChildren, "map" | "forEach" | "toArray"> & object;
```

ReactLynx children 工具的类型。

## 类型声明

### forEach()

```ts
forEach<T extends ComponentChild, R>(children: T | T[], fn: (child: T, index: number) => R): readonly R[] | null;
```

#### 类型参数

| 类型参数 |
| ------ |
| `T` *extends* `ComponentChild` |
| `R` |

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `children` | `T` \| `T`[] |
| `fn` | (`child`: `T`, `index`: `number`) => `R` |

#### 返回

readonly `R`[] \| `null`

### map()

```ts
map<T extends ComponentChild, R>(children: T | T[], fn: (child: T, index: number) => R): readonly R[] | null;
```

#### 类型参数

| 类型参数 |
| ------ |
| `T` *extends* `ComponentChild` |
| `R` |

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `children` | `T` \| `T`[] |
| `fn` | (`child`: `T`, `index`: `number`) => `R` |

#### 返回

readonly `R`[] \| `null`

### toArray()

```ts
toArray(children: ComponentChildren): readonly VNode<{
}>[];
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `children` | `ComponentChildren` |

#### 返回

readonly `VNode`\<\{
\}\>[]
