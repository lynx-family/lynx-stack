# 函数: findByTitle()

```ts
function findByTitle<T extends HTMLElement = HTMLElement>(...args: [HTMLElement, Matcher, MatcherOptions, waitForOptions]): Promise<T>;
```

## 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `T` *extends* `HTMLElement` | `HTMLElement` |

## 参数

| 参数 | 类型 |
| ------ | ------ |
| ...`args` | \[`HTMLElement`, [`Matcher`](../类型别名/Matcher.mdx), [`MatcherOptions`](../接口/MatcherOptions.mdx), [`waitForOptions`](../接口/waitForOptions.mdx)\] |

## 返回

`Promise`\<`T`\>
