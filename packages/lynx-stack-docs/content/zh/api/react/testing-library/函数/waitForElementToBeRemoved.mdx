# 函数: waitForElementToBeRemoved()

```ts
function waitForElementToBeRemoved<T>(callback: T | (() => T), options?: waitForOptions): Promise<void>;
```

## 类型参数

| 类型参数 |
| ------ |
| `T` |

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `callback` | `T` \| (() => `T`) |
| `options?` | [`waitForOptions`](../接口/waitForOptions.mdx) |

## 返回

`Promise`\<`void`\>
