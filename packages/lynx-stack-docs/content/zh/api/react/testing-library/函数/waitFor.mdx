# 函数: waitFor()

```ts
function waitFor<T>(callback: () => T | Promise<T>, options?: waitForOptions): Promise<T>;
```

## 类型参数

| 类型参数 |
| ------ |
| `T` |

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `callback` | () => `T` \| `Promise`\<`T`\> |
| `options?` | [`waitForOptions`](../接口/waitForOptions.mdx) |

## 返回

`Promise`\<`T`\>
