# 类型别名: NewPlugin

```ts
type NewPlugin = object;
```

## 属性

### serialize

```ts
serialize: (val: any, config: Config, indentation: string, depth: number, refs: Refs, printer: Printer) => string;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `val` | `any` |
| `config` | [`Config`](prettyFormat.Config.mdx) |
| `indentation` | `string` |
| `depth` | `number` |
| `refs` | [`Refs`](prettyFormat.Refs.mdx) |
| `printer` | [`Printer`](prettyFormat.Printer.mdx) |

#### 返回

`string`

***

### test

```ts
test: Test;
```
