# 类型别名: GetAllBy\<Arguments *extends* `any`[]\>

```ts
type GetAllBy<Arguments extends any[]> = QueryMethod<Arguments, HTMLElement[]>;
```

## 类型参数

| 类型参数 |
| ------ |
| `Arguments` *extends* `any`[] |
