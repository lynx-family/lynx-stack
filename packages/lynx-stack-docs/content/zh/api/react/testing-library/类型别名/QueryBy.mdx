# 类型别名: QueryBy\<Arguments *extends* `any`[]\>

```ts
type QueryBy<Arguments extends any[]> = QueryMethod<Arguments, HTMLElement | null>;
```

## 类型参数

| 类型参数 |
| ------ |
| `Arguments` *extends* `any`[] |
