# 类型别名: MatcherFunction

```ts
type MatcherFunction = (content: string, element: Element | null) => boolean;
```

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `content` | `string` |
| `element` | `Element` \| `null` |

## 返回

`boolean`
