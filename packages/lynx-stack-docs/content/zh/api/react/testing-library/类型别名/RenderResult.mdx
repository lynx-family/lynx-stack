# 类型别名: RenderResult\<Q *extends* [`Queries`](../接口/Queries.mdx) = *typeof* [`queries`](../命名空间/queries.mdx)\>

```ts
type RenderResult<Q extends Queries = typeof queries> = object & { [P in keyof Q]: BoundFunction<Q[P]> };
```

[render](../函数/render.mdx) 的返回结果

## 类型声明

### asFragment

```ts
asFragment: () => DocumentFragment;
```

#### 返回

`DocumentFragment`

### container

```ts
container: LynxElement;
```

### rerender

```ts
rerender: (ui: React.ReactNode) => void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `ui` | `React.ReactNode` |

#### 返回

`void`

### unmount

```ts
unmount: () => void;
```

#### 返回

`void`

## 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `Q` *extends* [`Queries`](../接口/Queries.mdx) | *typeof* [`queries`](../命名空间/queries.mdx) |
