# 类型别名: FindBy\<Arguments *extends* `any`[]\>

```ts
type FindBy<Arguments extends any[]> = QueryMethod<[Arguments[0], Arguments[1]?, waitForOptions?], Promise<HTMLElement>>;
```

## 类型参数

| 类型参数 |
| ------ |
| `Arguments` *extends* `any`[] |
