# 接口: SelectorMatcherOptions

## 继承

- [`MatcherOptions`](MatcherOptions.mdx)

## 属性

### collapseWhitespace?

```ts
optional collapseWhitespace?: boolean;
```

请改为使用 normalizer 配合 getDefaultNormalizer

#### 继承自

[`MatcherOptions`](MatcherOptions.mdx).[`collapseWhitespace`](MatcherOptions.mdx#collapsewhitespace)

***

### exact?

```ts
optional exact?: boolean;
```

#### 继承自

[`MatcherOptions`](MatcherOptions.mdx).[`exact`](MatcherOptions.mdx#exact)

***

### ignore?

```ts
optional ignore?: string | boolean;
```

***

### normalizer?

```ts
optional normalizer?: NormalizerFn;
```

#### 继承自

[`MatcherOptions`](MatcherOptions.mdx).[`normalizer`](MatcherOptions.mdx#normalizer)

***

### selector?

```ts
optional selector?: string;
```

***

### suggest?

```ts
optional suggest?: boolean;
```

为特定查询禁用建议

#### 继承自

[`MatcherOptions`](MatcherOptions.mdx).[`suggest`](MatcherOptions.mdx#suggest)

***

### trim?

```ts
optional trim?: boolean;
```

请改为使用 normalizer 配合 getDefaultNormalizer

#### 继承自

[`MatcherOptions`](MatcherOptions.mdx).[`trim`](MatcherOptions.mdx#trim)
