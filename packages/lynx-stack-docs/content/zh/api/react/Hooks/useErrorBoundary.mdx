# 函数: useErrorBoundary()

```ts
function useErrorBoundary(callback?: (error: any, errorInfo: ErrorInfo) => void | Promise<void>): [any, () => void];
```

捕获下方组件抛出的错误，并让该组件
渲染一个 fallback。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `callback?` | (`error`: `any`, `errorInfo`: `ErrorInfo`) => `void` \| `Promise`\<`void`\> |

## 返回

\[`any`, () => `void`\]

## 参阅

https://preactjs.com/guide/v10/hooks/#useerrorboundary
