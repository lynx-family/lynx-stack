# 接口: GenerateHandlers

## 方法

### emit()

```ts
emit(
   value: string, 
   type: number, 
   auto: boolean
): void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `value` | `string` |
| `type` | `number` |
| `auto` | `boolean` |

#### 返回

`void`

***

### node()

```ts
node(node: CssNode): void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `node` | [`CssNode`](../type-aliases/CssNode.mdx) |

#### 返回

`void`

***

### result()

```ts
result(): string;
```

#### 返回

`string`

***

### token()

```ts
token(
   type: number, 
   value: string, 
   suppressAutoWhiteSpace?: boolean
): void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `type` | `number` |
| `value` | `string` |
| `suppressAutoWhiteSpace?` | `boolean` |

#### 返回

`void`

***

### tokenBefore()

```ts
tokenBefore(
   prevCode: number, 
   type: number, 
   value: string
): number;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `prevCode` | `number` |
| `type` | `number` |
| `value` | `string` |

#### 返回

`number`
