# 类: OffsetToLocation

## 构造函数

### 构造函数

```ts
new OffsetToLocation(
   source: string, 
   startOffset?: number, 
   startLine?: number, 
   startColumn?: number
): OffsetToLocation;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `source` | `string` |
| `startOffset?` | `number` |
| `startLine?` | `number` |
| `startColumn?` | `number` |

#### 返回

`OffsetToLocation`

## 方法

### getLocation()

```ts
getLocation(offset: number, filename?: string): object;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `offset` | `number` |
| `filename?` | `string` |

#### 返回

`object`

##### column

```ts
column: number;
```

##### line

```ts
line: number;
```

##### offset

```ts
offset: number;
```

##### source

```ts
source: string;
```

***

### getLocationRange()

```ts
getLocationRange(
   start: number, 
   end: number, 
   filename?: string
): CssLocation;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `start` | `number` |
| `end` | `number` |
| `filename?` | `string` |

#### 返回

[`CssLocation`](../interfaces/CssLocation.mdx)

***

### setSource()

```ts
setSource(
   source?: string, 
   startOffset?: number, 
   startLine?: number, 
   startColumn?: number
): void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `source?` | `string` |
| `startOffset?` | `number` |
| `startLine?` | `number` |
| `startColumn?` | `number` |

#### 返回

`void`
