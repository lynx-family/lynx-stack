# 类型别名: DSWalkEnterOrLeaveFn

```ts
type DSWalkEnterOrLeaveFn = (node: DSNode) => void;
```

Definition syntax 的遍历回调

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `node` | [`DSNode`](DSNode.mdx) |

## 返回

`void`
