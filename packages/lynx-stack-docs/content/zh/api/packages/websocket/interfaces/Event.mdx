# 接口: Event\<T *extends* `string`\>

`Event` 接口的一种实现，用于包装给定的事件对象。

## 参阅

https://dom.spec.whatwg.org/#event

## 类型参数

| 类型参数 |
| ------ |
| `T` *extends* `string` |

## 属性

### type

```ts
type: T;
```

该事件的类型。

#### 参阅

https://dom.spec.whatwg.org/#dom-event-type
