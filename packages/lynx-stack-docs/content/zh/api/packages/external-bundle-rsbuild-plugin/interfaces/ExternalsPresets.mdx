# 接口: ExternalsPresets

external bundle 依赖的预设。

## 可索引

```ts
[presetName: string]: boolean | object | undefined
```

额外的自定义预设开关。

## 属性

### reactlynx?

```ts
optional reactlynx?: 
  | boolean
  | ReactLynxExternalsPresetOptions;
```

加载 ReactLynx 运行时 bundle，并配置其标准模块全局变量。
