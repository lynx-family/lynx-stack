# 函数: normalizeBundlePath()

```ts
function normalizeBundlePath(bundlePath: string): string;
```

规范化公共 bundle 路径，移除开头的斜杠。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `bundlePath` | `string` |

## 返回

`string`
