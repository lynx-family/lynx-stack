# 函数: useSignal()

## 调用签名

```ts
function useSignal<T>(value: T, options?: SignalOptions<T>): Signal<T>;
```

### 类型参数

| 类型参数 |
| ------ |
| `T` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `value` | `T` |
| `options?` | [`SignalOptions`](../interfaces/SignalOptions.mdx)\<`T`\> |

### 返回

[`Signal`](../classes/Signal.mdx)\<`T`\>

## 调用签名

```ts
function useSignal<T = undefined>(): Signal<T | undefined>;
```

### 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `T` | `undefined` |

### 返回

[`Signal`](../classes/Signal.mdx)\<`T` \| `undefined`\>
