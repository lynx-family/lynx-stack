# 接口: VanillaBundleFilenameContext

传给 Vanilla Lynx bundle 文件名函数的上下文。

## 属性

### entryName

```ts
entryName: string;
```

逻辑上的 Rsbuild 入口名称。

***

### lazyBundle

```ts
lazyBundle: false;
```

Vanilla Lynx 目前只生成 main card bundle。

***

### platform

```ts
platform: string;
```

Rsbuild 环境名称。
