# 类型别名: ReduceFn\<TData, TValue, TContext = [`List`](../classes/List.mdx)\<`TData`\>\>

```ts
type ReduceFn<TData, TValue, TContext = List<TData>> = (this: TContext, accum: TValue, data: TData) => TValue;
```

## 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `TData` | - |
| `TValue` | - |
| `TContext` | [`List`](../classes/List.mdx)\<`TData`\> |

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `this` | `TContext` |
| `accum` | `TValue` |
| `data` | `TData` |

## 返回

`TValue`
