# 函数: createRpcEndpoint()

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[], Return = void>(
   name: string, 
   isSync: false, 
   hasReturn: false
): RpcEndpointAsyncVoid<Parameters>;
```

### 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `Parameters` *extends* `any`[] | - |
| `Return` | `void` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `false` |
| `hasReturn` | `false` |

### 返回

[`RpcEndpointAsyncVoid`](../type-aliases/RpcEndpointAsyncVoid.mdx)\<`Parameters`\>

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[], Return = void>(
   name: string, 
   isSync: false, 
   hasReturn: true
): RpcEndpointAsync<Parameters, Return>;
```

### 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `Parameters` *extends* `any`[] | - |
| `Return` | `void` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `false` |
| `hasReturn` | `true` |

### 返回

[`RpcEndpointAsync`](../interfaces/RpcEndpointAsync.mdx)\<`Parameters`, `Return`\>

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[], Return = void>(
   name: string, 
   isSync: false, 
   hasReturn: true, 
   hasReturnTransfer: false
): RpcEndpointAsync<Parameters, Return>;
```

### 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `Parameters` *extends* `any`[] | - |
| `Return` | `void` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `false` |
| `hasReturn` | `true` |
| `hasReturnTransfer` | `false` |

### 返回

[`RpcEndpointAsync`](../interfaces/RpcEndpointAsync.mdx)\<`Parameters`, `Return`\>

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[], Return = void>(
   name: string, 
   isSync: false, 
   hasReturn: true, 
   hasReturnTransfer: true
): RpcEndpointAsyncWithTransfer<Parameters, Return>;
```

### 类型参数

| 类型参数 | 默认类型 |
| ------ | ------ |
| `Parameters` *extends* `any`[] | - |
| `Return` | `void` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `false` |
| `hasReturn` | `true` |
| `hasReturnTransfer` | `true` |

### 返回

[`RpcEndpointAsyncWithTransfer`](../interfaces/RpcEndpointAsyncWithTransfer.mdx)\<`Parameters`, `Return`\>

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[]>(
   name: string, 
   isSync: true, 
   hasReturn: false
): RpcEndpointSyncVoid<Parameters>;
```

### 类型参数

| 类型参数 |
| ------ |
| `Parameters` *extends* `any`[] |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `true` |
| `hasReturn` | `false` |

### 返回

[`RpcEndpointSyncVoid`](../type-aliases/RpcEndpointSyncVoid.mdx)\<`Parameters`\>

## 调用签名

```ts
function createRpcEndpoint<Parameters extends any[], Return>(
   name: string, 
   isSync: true, 
   hasReturn: true, 
   hasReturnTransfer: false, 
   bufferSize: number
): RpcEndpointSync<Parameters, Return>;
```

### 类型参数

| 类型参数 |
| ------ |
| `Parameters` *extends* `any`[] |
| `Return` |

### 参数

| 参数 | 类型 |
| ------ | ------ |
| `name` | `string` |
| `isSync` | `true` |
| `hasReturn` | `true` |
| `hasReturnTransfer` | `false` |
| `bufferSize` | `number` |

### 返回

[`RpcEndpointSync`](../interfaces/RpcEndpointSync.mdx)\<`Parameters`, `Return`\>
