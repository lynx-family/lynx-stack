# 函数: createLazyResolver()

```ts
function createLazyResolver(
   rspack: Rspack, 
   directory: string, 
   conditionNames: string[]
): (request: string) => Promise<string>;
```

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `rspack` | `Rspack` |
| `directory` | `string` |
| `conditionNames` | `string`[] |

## 返回

(`request`: `string`) => `Promise`\<`string`\>
