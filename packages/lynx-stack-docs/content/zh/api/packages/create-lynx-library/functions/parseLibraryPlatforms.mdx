# 函数: parseLibraryPlatforms()

```ts
function parseLibraryPlatforms(source: string): LibraryPlatform[];
```

从 CLI 输入中解析以逗号分隔的 Native 平台列表。

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `source` | `string` |

## 返回

[`LibraryPlatform`](../type-aliases/LibraryPlatform.mdx)[]
