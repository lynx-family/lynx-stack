# 类: MainThreadRuntimeWrapperWebpackPlugin

external bundle 的主线程运行时包装器。

## 构造函数

### 构造函数

```ts
new MainThreadRuntimeWrapperWebpackPlugin(options?: Partial<MainThreadRuntimeWrapperWebpackPluginOptions>): MainThreadRuntimeWrapperWebpackPlugin;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `options` | `Partial`\<[`MainThreadRuntimeWrapperWebpackPluginOptions`](../interfaces/MainThreadRuntimeWrapperWebpackPluginOptions.mdx)\> |

#### 返回

`MainThreadRuntimeWrapperWebpackPlugin`

## 方法

### apply()

```ts
apply(compiler: Compiler): void;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `compiler` | `Compiler` |

#### 返回

`void`
