# 接口: ExternalsPresetDefinition

具名 externals 预设的定义。

## 属性

### extends?

```ts
optional extends?: string | string[];
```

在应用当前预设之前需要包含的其他预设名称。

***

### externals?

```ts
optional externals?: Externals;
```

该预设提供的 externals。
