# 接口: MainThreadRuntimeWrapperWebpackPluginOptions

[MainThreadRuntimeWrapperWebpackPlugin](../classes/MainThreadRuntimeWrapperWebpackPlugin.mdx) 的选项。

## 属性

### test

```ts
test: Rules | undefined;
```

包含标记为 `lynx:main-thread` 且通过 test 断言的资源。

#### 默认值

`/\.js$/`
