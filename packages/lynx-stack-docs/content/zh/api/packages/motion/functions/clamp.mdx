# 函数: clamp()

```ts
function clamp(
   min: number, 
   max: number, 
   v: number
): number;
```

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `min` | `number` |
| `max` | `number` |
| `v` | `number` |

## 返回

`number`
