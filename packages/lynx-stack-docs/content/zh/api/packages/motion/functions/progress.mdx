# 函数: progress()

```ts
function progress(
   from: number, 
   to: number, 
   value: number
): number;
```

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `from` | `number` |
| `to` | `number` |
| `value` | `number` |

## 返回

`number`
