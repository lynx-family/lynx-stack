# 类: PanGesture

## 继承

- [`ContinuousGesture`](ContinuousGesture.mdx)\<[`PanGestureConfig`](../interfaces/PanGestureConfig.mdx), [`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\>

## 构造函数

### 构造函数

```ts
new PanGesture(gesture?: BaseGesture<PanGestureConfig, PanGestureChangeEvent>): PanGesture;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `gesture?` | [`BaseGesture`](BaseGesture.mdx)\<[`PanGestureConfig`](../interfaces/PanGestureConfig.mdx), [`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`PanGesture`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`constructor`](ContinuousGesture.mdx#constructor)

## 属性

### \_\_isGesture

```ts
__isGesture: true = true;
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`__isGesture`](ContinuousGesture.mdx#__isgesture)

***

### callbacks

```ts
callbacks: BaseGestureCallbacks<PanGestureChangeEvent> & ContinuousGestureCallbacks<PanGestureChangeEvent> = {};
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`callbacks`](ContinuousGesture.mdx#callbacks)

***

### config

```ts
config: PanGestureConfig;
```

#### 重写了

[`ContinuousGesture`](ContinuousGesture.mdx).[`config`](ContinuousGesture.mdx#config)

***

### continueWith

```ts
continueWith: BaseGesture<BaseGestureConfig, GestureChangeEvent>[] = [];
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`continueWith`](ContinuousGesture.mdx#continuewith)

***

### distanceSet

```ts
distanceSet: boolean = false;
```

***

### execId

```ts
execId: number;
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`execId`](ContinuousGesture.mdx#execid)

***

### id

```ts
id: number;
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`id`](ContinuousGesture.mdx#id)

***

### simultaneousWith

```ts
simultaneousWith: BaseGesture<BaseGestureConfig, GestureChangeEvent>[] = [];
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`simultaneousWith`](ContinuousGesture.mdx#simultaneouswith)

***

### toJSON

```ts
toJSON: (this: BaseGesture) => Record<string, unknown>;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `this` | [`BaseGesture`](BaseGesture.mdx) |

#### 返回

`Record`\<`string`, `unknown`\>

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`toJSON`](ContinuousGesture.mdx#tojson)

***

### type

```ts
type: GestureTypeInner = GestureTypeInner.PAN;
```

#### 重写了

[`ContinuousGesture`](ContinuousGesture.mdx).[`type`](ContinuousGesture.mdx#type)

***

### waitFor

```ts
waitFor: BaseGesture<BaseGestureConfig, GestureChangeEvent>[] = [];
```

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`waitFor`](ContinuousGesture.mdx#waitfor)

## 方法

### clone()

```ts
clone(): this;
```

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`clone`](ContinuousGesture.mdx#clone)

***

### enabled()

```ts
enabled(enabled: boolean): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `enabled` | `boolean` |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`enabled`](ContinuousGesture.mdx#enabled)

***

### externalContinueWith()

```ts
externalContinueWith(gesture: GestureKind): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `gesture` | [`GestureKind`](../interfaces/GestureKind.mdx) |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`externalContinueWith`](ContinuousGesture.mdx#externalcontinuewith)

***

### externalSimultaneous()

```ts
externalSimultaneous(gesture: GestureKind): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `gesture` | [`GestureKind`](../interfaces/GestureKind.mdx) |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`externalSimultaneous`](ContinuousGesture.mdx#externalsimultaneous)

***

### externalWaitFor()

```ts
externalWaitFor(gesture: GestureKind): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `gesture` | [`GestureKind`](../interfaces/GestureKind.mdx) |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`externalWaitFor`](ContinuousGesture.mdx#externalwaitfor)

***

### minDistance()

```ts
minDistance(distance: number): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `distance` | `number` |

#### 返回

`this`

***

### onBegin()

```ts
onBegin(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onBegin`](ContinuousGesture.mdx#onbegin)

***

### onEnd()

```ts
onEnd(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onEnd`](ContinuousGesture.mdx#onend)

***

### onStart()

```ts
onStart(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onStart`](ContinuousGesture.mdx#onstart)

***

### onTouchesCancel()

```ts
onTouchesCancel(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onTouchesCancel`](ContinuousGesture.mdx#ontouchescancel)

***

### onTouchesDown()

```ts
onTouchesDown(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onTouchesDown`](ContinuousGesture.mdx#ontouchesdown)

***

### onTouchesMove()

```ts
onTouchesMove(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onTouchesMove`](ContinuousGesture.mdx#ontouchesmove)

***

### onTouchesUp()

```ts
onTouchesUp(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onTouchesUp`](ContinuousGesture.mdx#ontouchesup)

***

### onUpdate()

```ts
onUpdate(cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`onUpdate`](ContinuousGesture.mdx#onupdate)

***

### overrideDefaultMinDistance()

```ts
overrideDefaultMinDistance(): this;
```

#### 返回

`this`

***

### serialize()

```ts
serialize(): Record<string, unknown>;
```

#### 返回

`Record`\<`string`, `unknown`\>

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`serialize`](ContinuousGesture.mdx#serialize)

***

### toGestureArray()

```ts
toGestureArray(): BaseGesture<BaseGestureConfig, GestureChangeEvent>[];
```

#### 返回

[`BaseGesture`](BaseGesture.mdx)\<[`BaseGestureConfig`](../interfaces/BaseGestureConfig.mdx), [`GestureChangeEvent`](../interfaces/GestureChangeEvent.mdx)\>[]

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`toGestureArray`](ContinuousGesture.mdx#togesturearray)

***

### updateCallback()

```ts
updateCallback(k: keyof BaseGestureCallbacks<TEvent> | "onUpdate", cb: GestureCallback<PanGestureChangeEvent>): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `k` | keyof BaseGestureCallbacks\<TEvent\> \| `"onUpdate"` |
| `cb` | [`GestureCallback`](../type-aliases/GestureCallback.mdx)\<[`PanGestureChangeEvent`](../interfaces/PanGestureChangeEvent.mdx)\> |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`updateCallback`](ContinuousGesture.mdx#updatecallback)

***

### updateConfig()

```ts
updateConfig(k: string, v: unknown): this;
```

#### 参数

| 参数 | 类型 |
| ------ | ------ |
| `k` | `string` |
| `v` | `unknown` |

#### 返回

`this`

#### 继承自

[`ContinuousGesture`](ContinuousGesture.mdx).[`updateConfig`](ContinuousGesture.mdx#updateconfig)
