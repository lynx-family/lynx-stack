# 函数: useGesture()

```ts
function useGesture<T extends IBasicGestures>(GestureConstructor: () => T): T;
```

## 类型参数

| 类型参数 |
| ------ |
| `T` *extends* `IBasicGestures` |

## 参数

| 参数 | 类型 |
| ------ | ------ |
| `GestureConstructor` | () => `T` |

## 返回

`T`
