# source.alias

```ts
optional alias?: Record<string, string | false | string[]>;
```



创建别名，以便更方便地 `import` 或 `require` 某些模块。

## 已被弃用

请使用 [Resolve.alias](/zh/api/packages/rspeedy/interfaces/Resolve.mdx#alias) 代替。

## 默认值

```ts
undefined
```

