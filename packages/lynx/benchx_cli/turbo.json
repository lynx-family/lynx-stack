{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["ECOSYSTEM_CI"],
      "passThroughEnv": ["ECOSYSTEM_CI"],
      "dependsOn": [],
      "inputs": ["scripts/build.mjs", "scripts/build_unix.sh"],
      "outputs": ["dist/**"]
    }
  }
}
