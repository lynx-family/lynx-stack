{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["ECOSYSTEM_CI"],
      "dependsOn": [],
      "inputs": ["scripts/build.mjs"],
      "outputs": ["dist/**"]
    }
  }
}
