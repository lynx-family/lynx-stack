# __PACKAGE_NAME__

Native Lynx library.

## Development

```bash
npm install
npm run codegen
```

The generated native specs are written to `generated/`,
__PLATFORM_DIRECTORY_LIST__. The package is discovered by Lynx through
`lynx.lib.json`.
