{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "package.json",
        "rslib.config.ts",
        "src/**",
        "tsconfig.json"
      ],
      "outputs": [
        "dist"
      ]
    }
  }
}
